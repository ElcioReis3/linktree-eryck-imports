import { ThemeButton } from "../theme-button";

export const Header = () => {
  return (
    <header className="relative w-full max-w-3xl text-center p-3 font-bold m-auto">
      CONHEÇA NOSSAS REDES SOCIAIS
      <ThemeButton />
    </header>
  );
};
